n = int(input('Nhap so phan tu cua mang: '))
a = []
for i in range(0, n):
    b = int(input('Nhap gia tri phan tu: '))
    a.append(b)
avg = sum(a) / n
print ('Gia tri trung binh cua mang', round(avg, 2))
